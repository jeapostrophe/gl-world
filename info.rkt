#lang setup/infotab
(define name "OpenGL World")
(define release-notes
  (list '(ul
          (li "Fixing contracts"))))
(define repositories
  (list "4.x"))
(define blurb
  (list "Like World, but with OpenGL rendering."))
(define scribblings '(("gl-world.scrbl" ())))
(define primary-file "main.rkt")
(define categories '(media ui))